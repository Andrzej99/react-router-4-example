{
  "name": "react-router-4-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cypress": "^3.4.0"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:open-admin": "cypress open --config integrationFolder=cypress/integration/administration",
    "cypress:admin": "cypress run --spec cypress/integration/administration/**/*"
  }
}
